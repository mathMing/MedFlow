# 2D 医疗图像分割示例 (如内窥镜息肉、皮肤镜病灶)
exp_name: "exp_2d_demo"
spatial_dims: 2
data_shape: [128, 128] # [H, W]
in_channels: 1
out_channels: 1

# 模型与训练
model_name: "unet"
base_filters: 32
use_monai: false

epochs: 5
batch_size: 4
lr: 0.001

train_samples: 30
val_samples: 6
